1-our goal is to create tables, adding data to them, and manipulate.
we first  install maven , which deals with dependencies etc.
we compile our code with 
mvn compile 
which gives us the syntax errors 
then we do nvm exec: java - .. one 
i did the first qt now 

H2 is a DBMS, runs ENTIRELY on the memory, we create a connection and run the query.
I need to study on the code structure tho. 


